# G1 — same-variant non-Plus lifecycle

Status: IN_PROGRESS
Candidate result date: 24 September 2026 UTC
Principal acceptance: M0-001 and M0-002 externally reviewed as partial checkpoints; G1 not passed; M0-003 pending

Current M0-002 continuation: [direct receipt index](../m0-001/evidence/m0-002/README.md). The M0-001 sections below are historical; the current outcome is recorded at the end.

## Claim and boundary

M0-001 tests whether one real garment variant can be expanded into exactly one child of the same variant at a fixed higher unit price, then follow Shopify's native development-store checkout, inventory, fulfillment, refund and restock lifecycle. The locked public-app, non-Plus requirement remains unqualified while app distribution is UNVERIFIED. A Basic development store has exceptional Function capabilities; local and development-store success cannot mark overall G1 PASS.

## Reproduction context

- Repository: `Optidigi/insignia`; branch `spike/m0-001-same-variant` from remote-main merge `0f87be149247ea6720984f2cefdc557423001661`. Final candidate commit belongs in PR metadata.
- Function API and generated schema: `2026-07`; schema SHA-256 `4f5024326918113b836e52af66991ca0fd69884e18ca74a07a92fd57e64c2789`.
- Shopify CLI `4.8.2`, `shopify_function` crate `2.2.0`, Rust `1.98.1`, Node `24.21.0`, pnpm `12.6.0`. Local native linker: isolated Zig `0.16.0` via explicit `CARGO_TARGET_X86_64_UNKNOWN_LINUX_GNU_LINKER`.
- Existing app `insignia`, OAuth client ID `942e6668fd1177524c0fc48b104b0ac3`; staging store `insignia-staging.myshopify.com`, shop `gid://shopify/Shop/78935261342`, Basic development store. Exact Dashboard numeric binding is owner-attested; distribution UNVERIFIED.
- Final locally checked release Wasm SHA-256 `5db1b7838409ef4a2a85dbd1ba976e1abb15f32c267a87ab4c006e7d6bac8d1e` (two repeat checks matched). Shopify's preview built the same source, but its uploaded binary hash is not exposed by the CLI, so this hash attests only the local artifact. The generated input/output fixtures use synthetic IDs and USD as a local matching presentment context; the installed app independently read shop currency USD.

## Acceptance matrix

| Case | Expected | Observed | Result | Evidence |
|---|---|---|---|---|
| Local empty/unmarked/unallowed/currency controls | No operations | 11 fixture suite passed, including negative controls | PASS (local only) | `spikes/m0-001/gates/nonplus-same-variant/fixtures/`; `corepack pnpm test` exit 0 |
| Local marked quantities 1 and 3, two variants and plain coexistence | One `lineExpand` per eligible line; one same-variant child with relative quantity 1 and exactly 3000 minor units | 11 schema-validated Wasm fixtures passed | PASS (local only) | `corepack pnpm test` exit 0 |
| Large local quantity 2000 | One operation and one relative child; no per-unit output loop | Wasm fixture and native predicate test passed | PASS (local only) | `cargo test --offline`, `corepack pnpm test` exit 0 |
| Real development-store app preview and transform | Staging-only app preview; owned same-variant Function with `blockOnFailure` | Preview Ready, installed app ID unchanged, seven actual scopes, Function uploaded and owned transform active with no userErrors | PASS (activation only) | [Sanitized activation transcript](../m0-001/evidence/staging-activation.md), [direct post-cleanup API response](../m0-001/evidence/post-cleanup.json); IDs below |
| Real Online Store cart and checkout | Exact fixture variant economics | Fixture had zero inventory and `onlineStoreUrl: null`; storefront redirected to `/password`. The designated stock location and authenticated native browser route were unavailable during this run. No cart or checkout was attempted. | BLOCKED (access/setup) | HTTP 200 redirect to `/password`; app-specific product/inventory API projection |
| Native fulfillment, refund and restock | Real variant IDs/quantities and correct stock/monetary basis | No checkout/order exists, so these lifecycle steps were not attempted | NOT_RUN (upstream blocked) | — |
| Public-app/non-Plus production qualification | Supported distribution and representative merchant behavior | Distribution unverified | NOT_RUN | Principal follow-through |

## Local artifacts and commands

`spikes/m0-001/scripts/check-local.sh` ran with the isolated Rust env and host linker: formatting, clippy, two native tests, release Wasm build and eleven Shopify test-helper fixture tests all exited 0. The first marked fixture went red against the generated no-op Function, then passed after implementation. The SDK serialized the exact whole-unit fixture price as `"30.0"`; tests parse decimal strings to exact integer cents and assert `3000`, with no floating tolerance.

`shopify app function run --path extensions/m0-001-same-variant --json` on the quantity-3 fixture exited 0 and reported `success: true`, `instructions: 45528`, `memory_usage: 1152`, output size `40`; these are basic local measurements, not a G2 budget or store capacity claim. The output had one `lineExpand`, one child of `gid://shopify/ProductVariant/111`, relative quantity `1`, and fixed unit amount `"30.0"`.

The first read-only fixed-ref review raised a two-document pnpm lockfile concern. pnpm 12 does support its generated environment+project documents, but a single-document lockfile is friendlier to common scanners. Project-local `pmOnFail: ignore` delegates pnpm version selection to Corepack's exact `packageManager` pin and generated one dependency lockfile document. A fresh scratch `corepack pnpm install --frozen-lockfile` exited 0 with 108 packages, and the full local check script passed again (two native tests, eleven Wasm fixtures). The reviewer must recheck this changed ref before handoff.

## Resource ledger and cleanup

The existing staging app installation `gid://shopify/AppInstallation/781307904158` retained its ID. The `app dev` preview reached Ready for `insignia-staging.myshopify.com` and auto-granted `read_cart_transforms`, `write_cart_transforms`, `write_products`, `read_products`, `write_inventory`, `read_inventory`, and `read_locations`. The last two read counterparts were granted by Shopify despite only five scopes in the project config. The app's Cart Transform list was empty immediately before fixture creation. No released app version or distribution method was selected.

The preview log also printed `APP_UNINSTALLED webhook delivery failed` while preparing. The subsequent app-specific Admin query succeeded with the same installation ID and the new scopes. The cause of that webhook warning was not established and it is not evidence that an unrelated installation was changed.

The [sanitized action transcript](../m0-001/evidence/staging-activation.md) preserves selected CLI result fields and records that they were transcribed after the run; SHA-256 `19303c020a630663ae1c5500ab62f3c517eaad93d7739063d0f222ab1f033c7a`. The [direct post-cleanup `--output-file` response](../m0-001/evidence/post-cleanup.json) is unedited output from a narrow query; SHA-256 `5fdcd5086ccbc2b617c4293e7aa22c5946c16173fcd201905132533ff74d3e67`. These artifacts contain no raw CLI session log or authenticated HTTP trace, so activation is evidenced by the retained result transcription plus independent post-cleanup state, not by a signed Shopify audit export.

An owned fixture product was created via this app on the designated store: `gid://shopify/Product/10294344482974`, handle `insignia-m0-001-fixture-20260924`, tags `insignia-m0-001` and `synthetic-fixture`. It started DRAFT and was then explicitly set ACTIVE; an app-specific read returned `onlineStoreUrl: null`, so it has no Online Store URL. Its Black/Small real variant is `gid://shopify/ProductVariant/50529053343902`, inventory item `gid://shopify/InventoryItem/52644294131870`, SKU `INS-M0-001-BLK-S`; its Black/Medium real variant is `gid://shopify/ProductVariant/50529054163102`, inventory item `gid://shopify/InventoryItem/52644294951070`, SKU `INS-M0-001-BLK-M`. Both now have catalog unit price `20.00` USD, tracked physical inventory, shipping required and inventory policy DENY. The initial Small variant was created at `0.00` and untracked, then updated; Medium was created with its final attributes. No location quantity or publication change has occurred. Two existing fulfillment-capable locations were returned, and the owner has been asked which is designated for this fixture before any location-specific mutation. The owned product must be archived/unpublished at cleanup. Record further exact IDs and before/after state here before cleanup; do not touch unrelated resources.

The dev preview uploaded Function `01a0d577-84c9-7b97-abb9-e5be7ef3fd2b`, handle `m0-001-same-variant`, `cart_transform` API `2026-07`, owned by the designated app key. After confirming no existing Cart Transforms, `cartTransformCreate` returned owned transform `gid://shopify/CartTransform/143098014`, `blockOnFailure: true`, no userErrors, and owned JSON metafield `gid://shopify/Metafield/70423168090270` allowlisting only the two fixture variants at fixed `30.00` USD. A subsequent app-specific query returned only this active transform. No released version was created.

Cleanup was verified: `cartTransformDelete` returned the exact owned transform ID and no userErrors; a subsequent query returned zero Cart Transforms. `productUpdate` archived the exact owned product, whose `onlineStoreUrl` remains null; both fixture variants remain at zero available/committed/on-hand inventory at the automatically assigned Shop location. The preview process was stopped, and `shopify app dev clean` exited 0 and reported restoration of the active version to this store. The two preexisting app version records stayed `insignia-2` active and `insignia-1` inactive. No carts or orders were created. The app installation and seven scopes remain on staging, as permitted; the archived fixture product remains for the evidence record. Distribution is still UNVERIFIED.

## Conclusion

Local Function shape and fixed-price output are proven against the generated 2026-07 schema and Wasm runner. The designated app accepted a staging-only preview, function upload and activation. Because product publication, fixture stock location, storefront access and checkout were unavailable, no real cart, order, inventory decrement, fulfillment, refund or restock result exists. This is an access/setup block, not a demonstrated failure of same-variant materialization. The development-store lifecycle and G1's public-app/non-Plus qualification remain open. G2–G8 are NOT_RUN.

## M0-002 continuation — current result

The exact PR #3 was merged normally after its attributed external approval and successful CI: remote-main merge `523efa4e0248cb2c64ed846e0b37af0067f01d9c`, whose parents are approved base `0f87be149247ea6720984f2cefdc557423001661` and head `a64f5939f9f9871e274868d4d9c10ed3dbbd890b`. The owner authorized M0-002 on the existing app/store/fixture and designated Shop location `gid://shopify/Location/89465290910`. The new branch started from that remote merge. The [M0-002 receipt index](../m0-001/evidence/m0-002/README.md), [manifest](../m0-001/evidence/m0-002/manifest.json), direct JSON/API requests, Function log projection and native UI captures carry the detailed evidence.

| Case | Observed result | Status |
|---|---|---|
| Plain/marked 1/3, edit/remove/re-add, mixed cart | Online Store Ajax and live Function logs supported USD 20 plain and USD 30 marked units. Mixed cart had six real fixture units at USD 170, with the plain same-variant line distinct. | PASS, development store |
| Checkout/order A | Bogus test gateway produced PAID test order `#1001`, `gid://shopify/Order/7184619962526`, USD 170 merchandise/total, with one plain Small at USD 20, three marked Small at USD 30, two marked Medium at USD 30, and preserved real variant/group IDs. | PASS, development store |
| Order reservation | From 12 available/0 committed/12 on_hand each, accepted A gave Small 8/4/12 and Medium 10/2/12 at Shop location. | PASS, development store |
| Native partial fulfillment A | The Admin form projected only marked Small quantity one selected; plain Small and marked Medium were unchecked. The native fulfillment `gid://shopify/Fulfillment/6511606562974` fulfilled **both** plain Small quantity one and marked Small quantity one. Small became 8/2/10; Medium stayed 10/2/12. | **FAIL: line identity and quantity** |
| Remainder fulfillment, refunds/restock, repeat order B/cancellation | Stopped after the dependent failure. No manual price, refund or stock correction was made. | NOT_RUN |
| Public-app/ordinary non-Plus qualification | Distribution remains unverified; development-store evidence cannot settle this. | NOT_RUN |

Cleanup deleted only this run's owned Cart Transform `gid://shopify/CartTransform/143130782`, stopped/cleaned its dev preview, and unpublished/archived the owned fixture. The staging installation and nine justified scopes remain, along with one partially fulfilled PAID Bogus test order, two Small and two Medium committed units, and residual stock. The password page remained enabled. No released app deployment occurred. A 36-assertion receipt consistency check passed while explicitly asserting the failure; it does not convert that semantic failure into a pass.

**G1 overall remains IN_PROGRESS**, with an observed same-variant native fulfillment failure for principal adjudication. No mechanism alternative, architecture change, G2–G8 execution or next slice is authorized by this record.

## M0-003 diagnostic continuation — current result

The principal [approved PR #4 as an evidence checkpoint](../../docs/delivery/PR-004-principal-review.md) at base/effective merge base `523efa4e0248cb2c64ed846e0b37af0067f01d9c` and head `1bfe9a13357b9c49042627f68fcca9daec93b1f5`. The normal merge `895aef65179267a9944c57c40478eeb704a8b65a` has exactly those parents. Approval did **not** pass G1 or establish a Shopify backend defect. The pre-submit M0-002 projection already said **“2 items selected”** even though the intended action was one marked Small. The original outgoing request is unavailable. Thus the intended-one/actual-two fulfillment remains a historical failed test with **undetermined cause**; its two-unit inventory movement matches the two fulfilled lines.

The [M0-003 direct evidence index](../m0-001/evidence/m0-003/README.md) records a new read-only app/API baseline and matching post-check read. Order #1001 and its fulfillment retain their exact prior IDs/quantities; Small remains 8 available/2 committed/10 on_hand and Medium 10/2/12 at the designated Shop location. No M0-003 Cart Transform, preview, order, stock adjustment or lifecycle mutation occurred. Storefront password protection and product archival remain.

A local pre-submit guard rejects the recorded selected-one/summary-two derived fixture and accepts a synthetic one-target/one-summary form. The guard explicitly does **not** authorize submission. The available collaborative browser can inspect page state and record video but does not expose the actual native POST body, request interception, HAR or CDP event stream. No supported scoped capture/abort route was established. Under the M0-003 stop rule, C and R purchases/fulfillments and optional B/dependent refund/restock tests are BLOCKED_CAPTURE/NOT_RUN rather than forced through an unverified native request. The principal must review this limit and decide whether a supported request-capture route can be supplied; no agent-selected architecture change follows.

**G1 remains IN_PROGRESS**; public-app/ordinary non-Plus qualification remains UNVERIFIED, and G2–G8 remain NOT_RUN.
