# G3 — authorization transport and verification

Status: **IN_PROGRESS**. M0-004 locally proves the candidate 114-byte payload + 64-byte Ed25519 signature (178 bytes, 238 canonical unpadded base64url characters) against independent Python/cryptography golden vectors. Node 24 and Rust agree on all 3 valid vectors and reject 22 malformed/mutated vectors; Rust uses strict Ed25519 verification. Pure complete-set tests cover signatures, key overlap, context, expiry, indexes, totals, optional/required policy, selling plans and 500-unit group removal. Both local Wasm targets independently verify the token. Review corrections added raw-byte ASCII parity, zero-variant rejection, D+2 signer enforcement, one key ID per set, marker-presence parity, a trusted physical-quantity bound and an allocation-to-issued-complete-set path. The candidate wire payload has no issuance-day claim, so offline verification cannot independently prove historical D; the signer must enforce it. See [contract](../m0-004/contract.md) and [vectors](../m0-004/fixtures/vectors.json).

No Ajax cart-property, expanded-child, checkout or order token round trip was run in M0-004. The 238-character transport through actual Shopify surfaces remains **UNVERIFIED**. Live app config/key rollout, including per-key validity windows, remains unimplemented. Protocol v1 is a candidate, not frozen; G3 is not PASS.

M0-004R appends 19 invalid cases without changing the original 3 valid or 22 invalid vectors: all 15 independently signed high-bit magic variants now fail at the header parser, a tampering control remains, and the pinned weak-key and valid-R/S+L scalar cases reject. Rust stores parsed, nonweak trusted keys and strictly verifies every complete signed member. Both verifiers enforce the independently supplied current day in `E-2..E`. The [corrected evidence](../m0-004/evidence/m0-004r/README.md) remains off-store and does not prove live transport or key rollout.
