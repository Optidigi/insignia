# G2 — complete Wasm budget

Status: **IN_PROGRESS**. M0-004 measured both local, complete signature/set/amount target paths against schema-valid synthetic 2026-07 inputs. See [M0-004 evidence](../m0-004/evidence/README.md) and [full measured rows](../m0-004/evidence/runner-bench.json).

The pinned runner executed and returned the expected outputs at 1, 10, 32 and 64 signed buckets. Execution above a documented limit is **failure**, even when the local runner returns output. With `opt-level=3`, four signed buckets use 9,436,004 Transform / 9,323,243 Validation instructions, below Shopify's 11,000,000 reference ceiling for up to 200 lines. Ten use 23,542,618 / 23,259,244 and fail that ceiling. At 200 cart lines, two signed buckets with 198 ordinary lines stay under the ceiling; three plus 197 ordinary lines exceed it. The 64-bucket Transform output is also 32,775 bytes, beyond the 20,000-byte reference ceiling. A 10,000-physical-unit case in five 2,000-quantity buckets exceeds the instruction ceiling without per-unit objects.

Binary, input/output and reported linear-memory results are recorded. Stack peak and numeric query cost are **UNKNOWN**. No product-facing capacity is accepted, and no deployed or live Function enforcement is claimed. The local capacity failure requires principal architecture/capacity adjudication; G2 is not PASS.

M0-004R retains those failed rows and adds a corrected baseline plus three bounded optimizations. The [final full-target evidence](../m0-004/evidence/m0-004r/README.md) still fails at 10 signed buckets (22,024,958 / 21,977,289 instructions), at five 2,000-unit buckets (11,092,566 / 11,070,692), and at 64-bucket Transform output (32,775 bytes). The new 42-row matrix includes mixed carts and adversarial controls; no accepted product capacity follows. Stack peak, numeric query cost and live enforcement remain unknown.
