# G5 — allocation and exact scalar boundary

Status: **IN_PROGRESS**. M0-004's fixed accepted-price allocator checks the EUR 91 split (2 × 30.33 + 1 × 30.34), one setup per group, stable variant reordering, heterogeneous prices, zero/one-minor setup, a two-group 500-unit quote totaling 1,458,500 minor units, 10,000 logical units without per-unit objects, and 128 deterministic conservation vectors. Wide u64 claims stay lexical. Rust's pinned SDK adapter emits exact decimal strings and reads Validation decimals through a String scalar override, with no `f64` economic comparison. The native target and postprocessed Wasm runner return exact strings in synthetic tests. See [M0-004 evidence](../m0-004/evidence/README.md).

The Validation query's `subtotalAmount` has not been proved to equal the independent pre-discount materialized child amount in live Shopify carts across discounts and Markets. No real allocated-bucket order/refund or contextual price source was tested in this package. Existing G1 native partial-refund receipts remain historical, with no new staging mutation. G5 is not PASS.
