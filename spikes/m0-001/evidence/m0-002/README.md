# M0-002 direct evidence — development-store lifecycle

Run: 24 September 2026 UTC. Gate G1 remains **IN_PROGRESS**. The real cart and test-only order passed their targeted checks, but native partial fulfillment **FAILED**: the Admin UI selected one marked Small and showed the plain Small unchecked; the resulting fulfillment included both. Dependent fulfillment, refund, restock and second-order cases stopped. This is a concrete same-variant merchant-operations failure for principal review, not an overall G1 verdict or permission to change architecture.

## Identity and execution

- Rewrite repository `Optidigi/insignia` (ID `1386102402`), branch `spike/m0-002-real-lifecycle` from verified remote-main PR #3 merge `523efa4e0248cb2c64ed846e0b37af0067f01d9c`. PR #3's external principal approval covered base/effective merge base `0f87be149247ea6720984f2cefdc557423001661`, head `a64f5939f9f9871e274868d4d9c10ed3dbbd890b`, and successful CI run `36068575630`. The merge commit has exactly those parents.
- Existing app `insignia`, OAuth client `942e6668fd1177524c0fc48b104b0ac3`; installed on `insignia-staging.myshopify.com`, shop `gid://shopify/Shop/78935261342`, Basic development store, USD. Installation `gid://shopify/AppInstallation/781307904158` stayed unchanged. Distribution remains UNVERIFIED.
- Owned product `gid://shopify/Product/10294344482974`; Small variant `gid://shopify/ProductVariant/50529053343902`, inventory item `gid://shopify/InventoryItem/52644294131870`; Medium variant `gid://shopify/ProductVariant/50529054163102`, inventory item `gid://shopify/InventoryItem/52644294951070`. Both catalog prices remained USD 20, tracked, shipping required, `DENY` oversell. Only Shop location `gid://shopify/Location/89465290910` received fixture stock and handled the order.
- `app dev` reached Ready for this app and shop. Actual scopes after reauthorization: `read_cart_transforms`, `read_inventory`, `read_locations`, `read_merchant_managed_fulfillment_orders`, `read_orders`, `read_products`, `write_cart_transforms`, `write_inventory`, `write_products`. No distribution choice or released deployment. Function handle `m0-001-same-variant`, API `2026-07`, preview Function ID `01a0d5c4-ef9f-71bb-9d4e-aa3165dfa3ad`. The new owned transform `gid://shopify/CartTransform/143130782` had `blockOnFailure: true`, two allowlisted fixture variants, USD 30.00 unit price and no creation userErrors.
- Local `CARGO_HOME`, `RUSTUP_HOME`, toolchain and explicit Zig host linker matched M0-001. `./scripts/check-local.sh` exited 0: formatting, clippy, two native Rust tests, release Wasm build and eleven schema-validated Wasm fixtures. Local Wasm SHA-256 stayed `5db1b7838409ef4a2a85dbd1ba976e1abb15f32c267a87ab4c006e7d6bac8d1e`. No remote uploaded-binary hash is exposed.

## Matrix and native economics

| Case | Expected | Direct observation | Result |
|---|---|---|---|
| Plain control | 1 Small, USD 20, no Function operation | Ajax USD 20; Function log has zero operations | PASS |
| Marked 1 then 3 | USD 30 then USD 90; same Small; physical quantity 1 then 3 | Ajax 3000 then 9000 minor units; Function output has one same-variant child, relative quantity 1 | PASS |
| Edit/remove/re-add | Correct key-targeted updates without duplicate expansion | `change.js` key edit to 3, removal to empty, `add.js` re-add; USD 90 each time | PASS |
| Mixed cart A | Marked Small 3, marked Medium 2, plain Small 1; six units, USD 170 | Three Ajax lines and native cart/checkout subtotal USD 170; plain same-variant line stayed USD 20 | PASS |
| Online Store order A | Test-only payment, real variants/quantities/prices, Shop location | `#1001`, `gid://shopify/Order/7184619962526`, `test: true`, `paymentGatewayNames: ["bogus"]`, PAID. Three order lines: plain Small 1 × USD 20, marked Medium 2 × USD 30, marked Small 3 × USD 30; two `LineItemGroup`s retain markers and real variant IDs. Tax USD 0, free Standard shipping, total USD 170. One fulfillment order at Shop location. | PASS |
| Native partial fulfillment A | Select only one marked Small | UI selection projection: plain Small **unchecked**, marked Small checked with quantity 1, Medium unchecked; Shopify's selection label nevertheless said “2 items selected”. `#1001-F1`, `gid://shopify/Fulfillment/6511606562974`, fulfilled **one plain Small plus one marked Small**. Native timeline says “2 items”. | **FAIL** |
| Remainder fulfillment, A refunds/restock, order B cancellation | Exact eligible units/money and no double restock | Stopped after the dependent partial-fulfillment quantity/identity failure. No refund or order B occurred. | NOT_RUN, dependent |
| Public-app/non-Plus qualification | Representative ordinary merchant capability | Distribution and production-like capability still unverified. | NOT_RUN |

The [native selection projection](fulfillment-A-partial-ui-before.json), [API fulfillment receipt](fulfillment-A-response.json), [post-action order read](order-A-after-partial.json) and [post-action inventory read](after-partial-A.json) make the failure reproducible. The plain line is `gid://shopify/LineItem/16953568723102`, no group, USD 20. The marked Small is `gid://shopify/LineItem/16953568788638`, group `gid://shopify/LineItemGroup/17090642078`, USD 30. Both appear at quantity one in the same fulfillment. This was Shopify's native Admin form and action; no line was manually dissolved, no price was rewritten and no inventory was adjusted after the order.

| Snapshot | Small available/committed/on_hand | Medium available/committed/on_hand | Interpretation |
|---|---|---|---|
| [Initial](baseline.json) | 0 / 0 / 0 | 0 / 0 / 0 | Before fixture setup |
| [After seed](after-seed.json) | 12 / 0 / 12 | 12 / 0 / 12 | Fixture setup, `+12` each at Shop location only |
| [Before order A](before-order-A.json) | 12 / 0 / 12 | 12 / 0 / 12 | Carts did not reserve stock |
| [After accepted A](after-order-A.json) | 8 / 4 / 12 | 10 / 2 / 12 | Six units committed once, no on-hand shipment yet |
| [After native partial](after-partial-A.json) | 8 / 2 / 10 | 10 / 2 / 12 | Two Small units shipped off hand; one was the unchecked plain line |
| [Post cleanup](post-cleanup.json) | 8 / 2 / 10 | 10 / 2 / 12 | No compensating adjustment; order remains partially fulfilled |

## Receipt provenance and cleanup

The `*.graphql`/`*.variables.json` files are the executed app API requests. Shopify CLI `4.8.2` wrote the narrow `*.json` response files directly with `app execute --version 2026-07 --output-file`; inspect `userErrors` as well as CLI exit status. [`inventory-seed-response.json`](inventory-seed-response.json) records the only fixture stock adjustment, using an idempotency key and a zero `changeFromQuantity` guard. [`transform-create-response.json`](transform-create-response.json), [readback](transform-readback.json), and [deletion response](transform-delete-response.json) identify only this run's transform. The first confirmation-number search returned an empty list; the later ID-specific query and native order page found the order. The cause of the empty search was not established. It did not change the order or its result.

[`function-runs-projected.json`](function-runs-projected.json) is a deterministic redacted projection of five Shopify CLI Function log files captured during the live cart/checkout actions. [`project-function-runs.py`](../../scripts/project-function-runs.py) records the transformation. Each projected run names and hashes its local raw source, hashes the opaque cart-line IDs for parent/output correlation, and retains all input lines and output operations; raw ignored `.shopify/logs/` files remain on this host and are not in Git. [`cart-mixed-A.png`](cart-mixed-A.png) is an unedited native storefront cart screenshot before entering buyer data. The browser `cart-*.json`, `checkout-A-*.json`, and `fulfillment-A-*-ui.json` files are narrow direct response/DOM projections captured at each action; they omit cart keys, checkout URLs, email, address and payment details. Hashes and timestamps establish file provenance, not a Shopify-signed receipt. The manifest records local filesystem birth times for retained receipts; these are host capture times, not Shopify server action timestamps or cryptographic attestations. The API order and fulfillment have their own server `createdAt` fields.

The password page was left enabled. The owner unlocked the authenticated shared storefront tab; the exact product route then showed both USD 20 variants and Add to cart. Admin published only Online Store and later unpublished it. A read of `publishedOnPublication` was denied to this app; `onlineStoreUrl` remained null in the app query even while the actual product page and checkout worked, so the browser and order receipts establish the tested channel behavior. The initial Pay now click surfaced address-field validation from browser automation; no order was created. Fields were corrected, and the successful payment used the built-in Bogus test gateway's documented `1` response. Shopify sent an automatic order confirmation to a synthetic `example.com` sink; marketing and fulfillment notifications were left off.

After capturing the failure, `cartTransformDelete` returned the exact owned transform ID with no userErrors, `app dev` quit, and `shopify app dev clean` exited 0 and reported restoring the active version. [Direct post-cleanup API output](post-cleanup.json) returned no transforms, the fixture `ARCHIVED` and unpublished, the same installation and nine scopes, and unchanged residual stock/commitments. [`versions-after.json`](versions-after.json) shows `insignia-2` still active and `insignia-1` inactive. Admin's unrelated Balloonie Demo preview remained present. The one paid Bogus test order, its successful two-unit fulfillment, four unfulfilled fixture units and stock commitments remain as evidence. No refund, cancellation, manual stock reconciliation, release, distribution change, password removal or second order occurred.

`python3 scripts/check-m0-002-evidence.py` passed 36 receipt-consistency assertions, including the **observed failure**. Its [result](verification.json) is an internal arithmetic/identity check, not a replacement for the native receipts. See `manifest.json` for hashes, IDs, run boundaries and per-case state. G1 overall stays IN_PROGRESS pending principal decision; G2–G8 remain NOT_RUN.
