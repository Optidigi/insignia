# M0-003R v1.1 — native C/R/B outcome

Run: 25 September 2026 UTC. [Outcome matrix](outcome.json); [receipt manifest](manifest.json). Capture mode is `NATIVE_UI_PLUS_PUBLIC_API_RECEIPTS`. Native Admin request bodies were unavailable and optional under the principal's prompt. The prior #1001 intended-one/actual-two discrepancy remains cause-undetermined.

## Authority and preconditions

The principal externally approved PR #5 at base/effective merge base `895aef65179267a9944c57c40478eeb704a8b65a` and head `afce3b668240d3a6b87741c173d1be0547ef4821`; Actions run `36079496495` passed. The owner delegated its exact normal merge and M0-003R staging envelope. Remote merge `a9398ebf8d069f3556c0b359be1372d94d45f82e` has those exact parents. Branch `spike/m0-003r-native-outcome` began there. No native GitHub approval was manufactured.

The [initial baseline](baseline.json), [protected order](protected-order.json) and [protected fulfillment](protected-fulfillment.json) match M0-003 JSON exactly. [Initial order inventory](order-inventory.json) contained only #1001. The fixture was archived and unpublished, no transform was active, and Shop location stock was Small available/committed/on-hand `8/2/10`, Medium `10/2/12`. The existing installation had nine scopes. The general Payments banner suggested activating a test provider, but the built-in **Test payment gateway** detail showed **Active** before and after reload, with a **Deactivate** action. The owner authorized activation only if needed; [no payment setting changed](readiness.json). `insignia-2` was active [before](versions-before.json) and [after](versions-after.json); no released deploy occurred.

Shopify CLI 4.8.2 `app dev` reached Ready on the named app/store under pinned Rust 1.98.1 and the project Zig linker. [Preview readback](preview-readback.json) tied the owned Function to the app. One allowlisted [Cart Transform was created](transform-create-response.json) and [read back](transform-readback.json): `gid://shopify/CartTransform/143294622`, two fixture variants, USD 30 unit price, `blockOnFailure: true`. Native Admin temporarily activated and published the fixture **only to Online Store**. The app API's `onlineStoreUrl` remained null while the actual product page, cart and checkout worked.

## Native comparison and lifecycle

| Case | Result | Principal evidence |
|---|---|---|
| C plain control | Test order [#1002](order-C-accepted.json): target Small 3 plus bystander Small 1, both USD 20, no group, $80. Native partial fulfillment `6512308158622` contained **one target unit only**; bystander remained unfulfilled. Native calculated $80 refund/restock returned Small stock to `8/2/10` and closed remaining commitments. | [Cart](cart-C.json), [checkout](checkout-C-prepayment.json), [native form](control-C-form.json), [cropped screenshot](control-C-pre-submit-crop.png), [guard mapping](control-C-guard.json), [fulfillment](fulfillment-C-after-partial.json), [refund/order](order-C-after-refund.json), [stock](stock-C-after-refund.json). |
| R expanded candidate | Test order [#1003](order-R-accepted.json): marked Small 3 plus marked Medium 2 at USD 30, plain Small 1 at USD 20, two expanded groups, $170. Native partial `6512343908510` contained **one marked Small only**. Remainder fulfillment `6512345579678` contained exactly plain Small 1, marked Small 2, marked Medium 2. Native calculated one-marked-Small refund was **$30 with one unit restocked**. Native calculated remaining **$140** refund had restock off and left stock unchanged. | [Cart](cart-R.json), [checkout](checkout-R-prepayment.json), [native form](candidate-R-form.json), [cropped screenshot](candidate-R-pre-submit-crop.png), [guard mapping](candidate-R-guard.json), [partial](fulfillment-R-after-partial.json), [remainder](fulfillment-R-after-remainder.json), [$30 form](candidate-R-one-refund-form.json), [$30 order/stock](order-R-after-one-refund.json), [$140 form](candidate-R-remainder-refund-form.json), [final order](order-R-after-final-refund.json), [final stock](stock-R-after-final-refund.json). |
| B unfulfilled cancellation | Test order [#1004](order-B-accepted.json): marked Small 3 at USD 30, $90, no fulfillment. Native cancellation selected `OTHER`, original Bogus payment refund $90, restock on, notification off. Fulfillment order closed, committed Small fell three and available rose three; on-hand did not move. | [Cart](cart-B.json), [checkout](checkout-B-prepayment.json), [native dialog](candidate-B-cancel-form.json), [post-cancel order](order-B-after-cancel.json), [stock](stock-B-after-cancel.json). |

Only three new successful test orders and 13 purchased units were created, exactly the authorized ceiling. The first C and R Pay-now interactions exposed address validation despite populated browser-tool inputs; native keyboard edits cleared it and direct order inventory was checked before retry. B's Pay-now tool reported a client error after the click, but the browser reached Thank you and [direct order inventory](after-order-B-inventory.json) showed exactly one new B order, so it was **not retried**. No checkout URL/token, credential, buyer details or full Admin screenshot is retained. Shopify sent automatic confirmations to synthetic `example.com` addresses; marketing and fulfillment/refund notifications were off.

The native `s-checkbox` is a Shopify web component. Read-only inspection showed that entering zero through the native quantity control deselected a bystander and removed its numeric input. The header's “N items selected” counts **selected physical units**: the fresh R form initially showed six across three rows. C and R settled at “1 item selected,” with one target numeric input at one, all bystanders deselected/zero, Shop location and notification off. The [adapted fail-closed guard](../../scripts/guard_native_selection.py) maps every native physical row by observed marker to the complete API line/FOL inventory and checks variant, quantity and summary. A group parent shown as “Part of” is presentation, not a second physical unit. The [official native procedure](https://help.shopify.com/en/manual/fulfillment/fulfilling-orders/single-fulfillment) uses desired item quantities. These observations support **this** procedure's outcome; they do not reveal the old request body or assign the historical failure to Shopify, `lineExpand` or the prior operator.

## Cleanup, verification and limits

The exact owned transform was [deleted with no errors](transform-delete-response.json). `app dev` exited; `shopify app dev clean --path . --client-id 942e6668fd1177524c0fc48b104b0ac3 --store insignia-staging.myshopify.com` reported **Dev preview stopped** and restored the active version. Native Admin unpublished Online Store, saved, then archived the fixture; reload showed **Archived** and **This product is not published anywhere**. [Post-cleanup API](post-cleanup.json) returned zero transforms, fixture `ARCHIVED`, unchanged installation/scopes, Small stock `5/2/7` and Medium `8/2/10`. Lower on-hand stock is honest residue of R's fulfilled units refunded **without** restock. No manual stock adjustment occurred. Three refunded test orders and three successful fulfillments remain as synthetic history. Protected [#1001 order](protected-order-after.json) and [fulfillment](protected-fulfillment-after.json) remain JSON-equal to initial reads. Unauthenticated `curl` to the staging root returned HTTP 302 to `/password`.

`python3 -B scripts/check-m0-003r-outcome.py` reconciles money, exact fulfillment multisets, refunds, stock deltas, protected equality, budget and cleanup from direct receipts. Six native-projection guard tests cover C/R mappings, missing mapping, bystander selection and summary mismatch. `./scripts/check-local.sh` includes these with Rust format/clippy/native tests, Wasm build, Vitest, historical checkers and all manifests. The [manifest](manifest.json) hashes every receipt. Historical manifests, source Function, schema, pricing, architecture plan and decision ledger are unchanged.

**Outcome:** `nativeWorkflowOutcome=PASS_OBSERVED_PROCEDURE`; `requestCapture=UNAVAILABLE_NOT_REQUIRED_FOR_THIS_TEST`; `historicalRootCause=UNDETERMINED`; `publicAppQualification=UNVERIFIED`; `gateStatus=G1_IN_PROGRESS`. A Basic development-store run does not establish ordinary public-app/non-Plus qualification or universal correctness. Native child-agent smoke failed before spawning (`no thread with id`); restricted sequential reviews are the permitted independent path. Principal review of this outcome PR is pending. M1, G2–G8, released deployment, distribution choice, extra scope and next-PR merge remain unauthorized.
