# M0-003 diagnostic checkpoint — native request capture blocked

Run: 25 September 2026 UTC. G1 remains **IN_PROGRESS**. This continuation added a tested fail-closed form guard, verified the protected baseline, and assessed the shared browser's request-capture capability. **No new carts, test orders, fulfillments, refunds, stock adjustments, fixture publication, app preview or transform were created.** The control/candidate comparison remains BLOCKED before a new payment or fulfillment mutation because the available shared-browser route cannot capture and correlate an outgoing native fulfillment request. The historical cause remains undetermined. This is a diagnostic/evidence checkpoint for principal review, not a claim that Shopify or `lineExpand` caused the M0-002 discrepancy.

## Authority and fixed resources

`Optidigi/insignia` PR #4 was merged normally after the external principal's SHA-bound approval and the owner's exact-PR delegation. Remote `main` merge `895aef65179267a9944c57c40478eeb704a8b65a` has parents approved base `523efa4e0248cb2c64ed846e0b37af0067f01d9c` and head `1bfe9a13357b9c49042627f68fcca9daec93b1f5`; its tree was fetched and verified before branching. `spike/m0-003-fulfillment-diagnosis` starts there. Native GitHub approval was not posted (HTTP 403); the principal verdict is the attributed external document under `docs/delivery/`.

[Direct app-specific baseline](baseline.json), [protected order read](protected-order.json) and [protected fulfillment read](protected-fulfillment.json) were saved by Shopify CLI `4.8.2`, Admin GraphQL `2026-07`, with `app execute --path . --client-id 942e6668fd1177524c0fc48b104b0ac3 --store insignia-staging.myshopify.com --version 2026-07 --query-file ... --output-file ...`. Executed query/variable inputs are adjacent. CLI exit 0 was checked alongside returned fields. The installed app ID remains `gid://shopify/AppInstallation/781307904158` with the same nine scopes; no conflicting transform was visible. Shop `gid://shopify/Shop/78935261342` is USD. Product `gid://shopify/Product/10294344482974` remains ARCHIVED, catalog USD 20 for the same two tracked/shipping-required `DENY` variants, with inventory levels only at designated Shop location `gid://shopify/Location/89465290910`. Unauthenticated GET of the product route resolved to `/password` (HTTP 200). Shared Admin `tab_8` was authenticated for the named product and protected order; no protected-order form was submitted or edited. The current Payments settings view did not establish a current active Bogus gateway; only the historical test order proves its prior use.

| Protected state at read | Small available/committed/on_hand | Medium available/committed/on_hand |
|---|---:|---:|
| 25 September baseline | 8 / 2 / 10 | 10 / 2 / 12 |

Order `#1001`, `gid://shopify/Order/7184619962526`, remains test=true, PAID and PARTIALLY_FULFILLED. Fulfillment `gid://shopify/Fulfillment/6511606562974` still contains exactly one plain Small and one marked Small. Fulfillment order `gid://shopify/FulfillmentOrder/8330797252766` remains at Shop location: plain Small FOL `17215047270558` has 0 remaining, marked Small `17215047303326` has 2, and marked Medium `17215047336094` has 2. The [19-check baseline result](verification.json) asserts these read-only facts and exact equality of the later [shop/fixture](post-check.json), [order](protected-order-post.json) and [fulfillment](protected-fulfillment-post.json) reads. No mutation to #1001 or its commitments occurred.

## Historical discrepancy and discriminators

The retained [M0-002 pre-submit projection](../m0-002/fulfillment-A-partial-ui-before.json) reports plain Small unchecked with quantity 1, marked Small checked with quantity 1, Medium unchecked, and aggregate **“2 items selected.”** The original outgoing fulfillment request was not among the historical 44 retained receipts. Narrow searches of local provider/browser logs found no retained original request body. This absence is a capture limit, not proof of what Shopify received. The principal reviewed the discrepancy as an intended-one/actual-two failure with undetermined cause; the inventory delta matches two units actually fulfilled.

| Hypothesis | Observable discriminator | M0-003 result |
|---|---|---|
| Locator/projection or uncommitted automation edit | Re-read real control properties and numeric value after native input/change/blur; compare actual outgoing request | NOT_RUN: no new order/form; old projection alone insufficient |
| Quantity-based form retains bystander despite unchecked appearance | Set bystander to zero through native controls; aggregate and request become exactly one | NOT_RUN; old positive bystander value/aggregate make this a live possibility |
| Same-variant or group UI selection issue | Plain duplicate-variant C succeeds coherently; expanded/plain R fails at form/request boundary | NOT_RUN: C/R not purchased |
| Correct one-target request processed into extra line | Sanitized actual request maps one FOL/line/quantity, but direct fulfillment response maps two | NOT_RUN: original request unavailable; no new sent request |

[Shopify's native partial-fulfillment guide](https://help.shopify.com/en/manual/fulfillment/fulfilling-orders/single-fulfillment) instructs entering the desired item quantities. This is the reason the guard treats positive non-target effective quantity as unsafe even when a checkbox appears unchecked. The guide does not establish this incident's cause.

## Guard, capture limit and stop

The [derived historical guard fixture](../../gates/fulfillment-diagnosis/historical-inconsistent-form.json) combines the recorded M0-002 UI summary with **line mappings from the direct order receipt**; its disabled/indeterminate/ariaChecked/inputValue fields are synthetic because the original projection did not capture them. It is a regression input, not a captured original request or complete DOM record. `python3 -B scripts/guard_fulfillment.py --form gates/fulfillment-diagnosis/historical-inconsistent-form.json` exits 2 and reports non-target positive quantity, unchecked positive quantity, identity/quantity mismatch and aggregate mismatch. The synthetic [one-target control](../../gates/fulfillment-diagnosis/coherent-control-form.json) exits 0. The CLI always returns `submissionAuthorized:false`, even for a coherent form; an actual correlated native request is a separate prerequisite. Missing checked/disabled/indeterminate/aria state or stale input value fails closed. These are local assertions, not observations of a new Shopify native form.

[Browser capability probe](capture-capability.json) records the actual collaborative tab and tool surface: page evaluation/snapshot/click/type/recording are available, but no network route, CDP event, HAR or request-body interception tool is exposed. `PerformanceResourceTiming` exposes URL/timing/status fields but no POST body. A recording probe produced MP4 video only, not request data; the private sample is excluded from Git. The browser process uses a remote-debugging pipe owned by the host's Playwright process, with no supported attached interception route. In-page replacement of `fetch`/XHR, service-worker interception, broad network export or credential/session copying would alter native behavior, collect broader authenticated traffic or bypass the authorized browser boundary. None was used.

Consequently the required actual native operation, line IDs and quantities cannot be checked before a fulfillment submit with the currently available tool surface. The prompt says to stop mutation when the payload cannot be safely recognized and correlated. No C/R/B payment or fulfillment was initiated, avoiding another paid test order with an unverifiable follow-up operation. C, R, optional B, native remainder fulfillment/refunds/restock/cancellation, and their monetary/inventory assertions are **BLOCKED_CAPTURE** or **NOT_RUN_DEPENDENT**. This does not establish a merchant-operation success or a fresh failure.

`./scripts/check-local.sh` exited 0 under the isolated Rust 1.98.1/Zig linker: rustfmt/clippy, two native tests, unchanged release Wasm SHA-256 `5db1b7838409ef4a2a85dbd1ba976e1abb15f32c267a87ab4c006e7d6bac8d1e`, eleven Wasm fixtures, five Python tests, historical 36-assertion receipt check, new 19-assertion protected-baseline check, and exact file/length/hash verification of 44 historical plus 16 new evidence files. No Shopify operation occurs in CI. The remote uploaded Function binary hash remains unavailable.

The existing fixture remains archived/unpublished; no M0-003 transform or preview exists to delete, and no M0-003 stock/order residue was created. The four historical commitments remain. No extra scopes, deployment, distribution setting, password change or global browser/security setting was made. Public-app/ordinary non-Plus qualification remains UNVERIFIED, G1 stays IN_PROGRESS and G2–G8 stay NOT_RUN. A supported scoped browser request-interception capability, or a principal-approved equivalent capture route that keeps request payloads private and permits abort-only inspection, is the precise dependency for a native comparison. The current PR does not grant that capability or authorize a next slice.
