**No concrete defect found** in correction delta `752ad127..9563de9`. The initial Standards finding is closed: the independent Python vector keeps the valid signature’s payload and R, then adds the correct 32-byte Ed25519 group order L to S. Reducing that S modulo L reproduces the valid signature, so this test distinguishes noncanonical rejection from a mismatched-signature rejection. The original 3 valid and 22 invalid vectors remain an exact prefix. The pinned Node probe passed; the [Rust test](/home/serveradmin/insignia-m0-004-worktree/spikes/m0-004/rust/authorization/tests/authorization.rs:256) uses the same positive control and `S+L` rejection, with its pass recorded in the retained log.

The [VerificationKey constructor](/home/serveradmin/insignia-m0-004-worktree/spikes/m0-004/rust/authorization/src/lib.rs:183) admits a parsed, nonweak key through private fields and keeps no separate raw cache. The [complete-set path](/home/serveradmin/insignia-m0-004-worktree/spikes/m0-004/rust/authorization/src/lib.rs:339) strictly verifies every signed member before acceptance.

The final source, runner and row-file hashes match the [capacity manifest](/home/serveradmin/insignia-m0-004-worktree/spikes/m0-004/evidence/m0-004r/capacity-manifest.json:70). All 16 smoke and 42 benchmark rows have zero runner errors; failed flags remain correct. Ten signed buckets use **22,024,958 / 21,977,289** instructions against 11M, and the 64-bucket Transform emits **32,775** bytes against 20,000.

This was a read-only review. I ran independent Python checks and the pinned Node probe; Rust tests and clean CLI builds were assessed from retained evidence. The current on-disk Wasm files match the separately documented repeat-build hashes, so the clean-final binary hashes were not independently rehashed. Stack peak, query cost and live platform behavior remain unverified. **No gate or PR approval.**