# M0-007 bounded policy and checkout evidence

All shop receipts here concern the existing `insignia` app and `insignia-staging.myshopify.com`. `browser-observations.json` transcribes narrow native Admin and guest-storefront observations; the GraphQL responses and projected Shopify CLI Function logs are direct receipts. The order ceiling was **one** successful Bogus order and **three** purchased units. The original M0-006 discount expectation and halt remain unchanged in their historical directory.

| Question | Direct result | Evidence |
|---|---|---|
| Correct starting identity and stock | Existing app/shop/fixture; archived, no owned Functions or M0-007 metadata; Small `5/2/7`, Medium `8/2/10` at Shop location | `baseline.json`, `metadata-before.json`, `protected-orders-before.json` |
| Publication and runtime controls | One owned Transform and one enabled Validation, both `blockOnFailure:true`; dedicated public key config and two product anchors projected into real Function input | `functions-active-readback.json`, `metadata-ready-readback.json`, `first-cart-transform.json`, `first-cart-validation.json` |
| Required-policy failure probes | Missing, malformed and stale policy with surviving registration rejected at checkout; unsigned known-required rejected; explicit optional plain accepted. No negative case was paid. | `policy-{missing,malformed,stale}-checkout-{transform,validation}.json`, `required-unsigned-checkout-{transform,validation}.json`, `optional-plain-checkout-{transform,validation}.json` |
| Validity and renewal | Independently signed expired and future-window offers rejected; current-day complete quote accepted with exact `60.66/30.34` pre-discount line amounts. Shop-local date, country, market and fresh public keys were taken from actual Function projection. | `issued-offers.json`, `live-context.json`, `{expired,future,current}-checkout-{transform,validation}.json` |
| Cart repair and express | Two customized Small lines removed by their distinct cart keys; one ordinary optional Medium remained and checked out for USD20.00 after only the orphan quote was cleared. Empty cart with stale quote redirected away from checkout. No cart express button was present; Product Buy Now historical safe block is not a compatibility pass. | `cart-repair-optional-{transform,validation}.json`, `browser-observations.json`, M0-006 historical accelerated result |
| Exact money and only successful order | Native checkout and Order #1006 show `2×30.33 + 1×30.34 = 91.00` pre-discount; native allocations `6.06 + 3.03 = 9.09`; paid Bogus merchandise `81.91`, free shipping, zero tax. Validation input retained the exact original `60.66/30.34`; it did not verify net discount math. | `discounted-checkout-{transform,validation}.json`, `order-1006-before-cancel.json`, `order-1006-discounts.json`, `browser-observations.json` |
| Native refund/restock | Admin calculated cancellation refunded `54.60 + 27.31 = 81.91`, `CANCEL` restock quantities `2+1`; final `REFUNDED`, current total zero; Small stock restored `5/2/7`, Medium unchanged `8/2/10`. No manual inventory/refund edit. | `order-1006-refunds.json`, `order-1006-after-cancel.json`, `after-cancel-baseline.json` |
| Cleanup and protected history | Exact owned Functions and three app-owned keys deleted, discount deleted, fixture unpub/archived, preview stopped, ephemeral private key directory removed, guest carriers cleared, password retained. #1001–#1005 status readback unchanged. | `validation-delete.json`, `transform-delete.json`, `metadata-final-delete.json`, `final-metadata.json`, `final-baseline.json`, `final-orders.json`, `browser-observations.json` |

`preview-bundle-identity.json` hashes the filtered, retained dev-preview base64 and decoded Wasm; the Wasm was byte-equal to its contemporaneous local build. `live-replay.json` records **20** exact-output and instruction-count matches when retained actual Shopify Function inputs were rerun against those exact preview Wasm bytes. Its per-case receipt and raw-event hashes bind the comparisons. `candidate-replay.json` and `fixtures/` retain **11** schema-valid synthetic full-target cases with instruction, input/output and binary measurements. `scripts/check-local.sh` preserves the frozen historical M0-006 source/receipts, runs local Rust/Function tests and both replays, and writes separate rebuilt-CI manifests. Rebuilt Wasm hashes may differ from retained preview bytes, so neither is silently substituted for the other.

The strongest observed policy result is **one-field-loss rejection** on this fixture. Both product anchors disappearing makes an unsigned required product observationally identical to unmanaged merchandise; no production-wide fail-closed guarantee or G6 pass follows. The first live Function projection also required an active/published fixture because an archived product could not enter an Ajax cart; the ideal pre-exposure projection condition was not fully provable in this dev-store procedure. Query cost is calculated from documented field rules, not a provider-returned numeric value; no live 200-line or catalogue-scale test occurred. See `../policy-contract.md`, `../resource-analysis.md` and the principal review packet.
