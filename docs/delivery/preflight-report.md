# PF-001 / PF-001R preflight report

## PF-001R remediation — 24 September 2026

**Readiness: BLOCKED for M0-001. Principal review: PENDING.** The approved rewrite repository now has a real `main` base; the docs/config PR URL and final SHA-bound publication status are recorded in the external handoff after this document commit. The PF-001 snapshot below is retained as the original checkpoint, and its older `MISSING` entries are superseded by this section. PF-001R is the only current authorization. M0-001 and G1–G8 have not run and are not authorized.

The exact source checkpoint was confirmed as `7f7fbabbfb60bd0e3d693d332e2b7da59693b9dd` on clean `local/pf-001-preflight`, with no remote. No subsequent work was discarded. The plan and ledger are byte-identical to the supplied zip at SHA-256 `c0432288deb778c4d717871d6f771f95e401d00b6a3caf1b878155cbf0e5422d` and `0c6c02bab83fb5032548c3d1be1f86ea1492ae3cfd26b6cacc8410f7d08737bb`. The [PF-001R probe record](evidence/pf001r-probes.md) contains commands, versions, results and limits. The original [PF-001 probes](evidence/pf001-probes.md) remain valid for the unchanged disposable Git, Node/TS, pnpm, developer MCP and browser checks.

| Capability | Current observation | Effect |
|---|---|---|
| Rust/Cargo/rustfmt/clippy/Wasm | **VERIFIED** in isolated `/home/serveradmin/insignia-pf001-tools/rust`: official rustup 1.29.1 installed pinned Rust 1.98.1 and `wasm32-unknown-unknown`; generic Wasm compile, rustfmt and clippy passed. The isolated `CARGO_HOME`/`RUSTUP_HOME` must be set. `cc` linker absent. | Removes the missing generic Wasm-toolchain blocker; Shopify Function behavior remains untested. |
| Shopify CLI | **VERIFIED** locally at pinned 4.8.2; version/help passed; no authenticated store sessions. | CLI binary ready; app/store identity and access remain blocked. |
| Fresh read-only Codex | **FAILED**: actual fresh CLI session could not read even AGENTS/ledger/skill because `bwrap` loopback setup returned `Operation not permitted`. `/usr/bin/bwrap` absent and user-namespace test failed; diagnosis is in probe record. | Isolated fresh review and sequential fallback are not demonstrated. A host owner must repair the sandbox; no permission broadening was applied. |
| Orchestrator model/effort and instructions | Project-local `.codex/config.toml` requests `gpt-6-sol`/`high` and disables native parallel delegation; explicit CLI flags were accepted. `codex doctor` still reports model `<default>`, no feature override; launched JSON exposes no wire model/effort. Prompt diagnostics load root AGENTS and four skills; live isolated file reads fail. | Effective sol-6-high and project config loading are **NOT_VERIFIED**. Do not infer model/child inheritance from a requested flag or this chat's self-description. |
| GitHub destination/write identity | User designated `Optidigi/insignia` and explicitly authorized a minimal initial `main` commit. `gh` is authenticated as `shimmy-aga`, with repository `ADMIN` permission. The separate clone at `/home/serveradmin/insignia-rewrite-20260924` tracks that URL. A README-only commit `38a711ad46aec63b8f410519f30352a88e4113c7` was pushed to `main`; remote branch/HEAD checks agree. No rulesets exist and the branch is unprotected. | Base and actual write path are **VERIFIED**; docs/config PR publication and its final refs are reported externally. Native independent review/merge enforcement is not established; the principal verdict remains external and the user retains merge authority. |
| Shopify test context | User designated app name `insignia` and the `insignia-staging` development store. CLI store sessions remain empty. `shopify organization list --json` in noninteractive mode exited 1 because authentication is required. | App ID, exact store domain, distribution/plan/privileges and read access remain **NOT_VERIFIED**. No store was guessed or mutated. |
| PostgreSQL, R2, billing | No new access. PostgreSQL is **NOT_YET_REQUIRED** for the smallest fixed-input, DB-free M0-001 lifecycle spike, but remains required for DB-dependent work; R2 and pricing remain later prerequisites. | No architecture/database decision changed. |

The previous local reviewer was a collaboration child in the API thread's `danger-full-access` runtime. It reviewed the PF-001 material, not a verified remote base/head, and its read-only instruction was not sandbox-enforced. Its no-finding feedback is retained without calling it an isolated fixed-ref review. A fresh fixed-ref local review of remediation commit `297002a948a065a76d6e9698758df4f4c26b2f11` found shortened command evidence and stale state wording; both were corrected in a follow-up commit. The final refs and review outcome are recorded in the external export manifest. This review runtime also had no enforced read-only sandbox.

**Owner actions, batched:** (1) host maintainer repairs Codex's Linux read-only sandbox, starting with a distribution `bubblewrap` install at `/usr/bin/bwrap` and verifying the AppArmor profile/user namespace, without disabling restrictions, and establishes observable model/effort/project trust; (2) user authenticates the Shopify CLI in the host's credential mechanism for the designated non-production app/store so the orchestrator can run read-only identity and privilege checks. These actions block M0 readiness as indicated; PostgreSQL, R2 and billing are deferred. No secret should be sent in chat.

The local review export and final commit/ref manifest are outside the tracked tree to avoid self-referential hashes. The PR publication outcome is recorded in that external handoff. No application scaffold, store mutation, deployment, production access, paid action or merge occurred.

24 September 2026, 17:20 UTC. Verdict: **BLOCKED** for M0-001 readiness and remote publication. This is a local-only documentation/configuration bootstrap. PF-001 remains the only authorized slice. Product decisions and architecture are unchanged; G1–G8 remain **NOT_RUN**.

## Workspace and publication

- Input archive at `/home/serveradmin/.t3/userdata/attachments/81292d0a-995f-4c06-8ce7-7d7cd419bc4a-ba802f63-f705-4c8a-ab8e-357ff8327bbc-zip.zip`; SHA-256: `646eabf045e71296c7fb962b0bb2306f562eaee2a0a5f8caec7da5f78a62e0ea`. Extracted to a neutral `/tmp/insignia-pf001-*` directory after checking member paths. `sha256sum -c MANIFEST.sha256` passed for all 16 supplied records **before edits**. Original `MANIFEST.sha256` and `SAVE-RECEIPT.json` are preserved. Current record hashes, including the two operational files updated for PF-001, are in [pf001-current-sha256.txt](evidence/pf001-current-sha256.txt). The manifest verifies the transfer snapshot, not later approved edits.
- Persistent local save: `/home/serveradmin/insignia-pf001-local-20260924`. It is a neutral handoff workspace, **not** the rewrite destination. It contains the original plan, ledger, operating model, templates, agent instructions, prompt and PR template at their supplied paths. The architecture plan and decision ledger retain source hashes `c0432288deb778c4d717871d6f771f95e401d00b6a3caf1b878155cbf0e5422d` and `0c6c02bab83fb5032548c3d1be1f86ea1492ae3cfd26b6cacc8410f7d08737bb`.
- Initial CWD `/home/serveradmin` had no Git root, branch or remotes. A search found no local `.git` directory under the working home tree. The unrelated `component-catalog` directory is also not a Git repository. The local handoff had no `.git`; no existing repository or Git history was overwritten.
- The connected GitHub app lists `Optidigi-AK/Insignia-shopify-app`, `Optidigi-AK/Insignia-app-docs` and older logo-printing projects. The first two READMEs describe documentation sources, and the older plans use a different stack. None is evidence that the user approved it as the greenfield rewrite destination. Their reported default branches are `main`; that metadata does not supply an approved PR base. No repo was selected, cloned or written.
- **Publication: LOCAL_ONLY.** PR URL, remote base SHA, remote head SHA and effective merge-base SHA are unavailable. A clean local commit and patch are supplied with the handoff outside this report, avoiding a self-referential commit SHA. Local `gh` is unauthenticated. Connected app repository permission metadata includes `push`, but no branch or PR write was attempted without a verified, user-approved destination. CI workflows and workspace scripts are **NOT_CREATED**.

## Agent host, instruction chain and roles

- Codex CLI `0.156.1` at `/home/serveradmin/.local/bin/codex`; `codex doctor --json` reports provider `openai`, ChatGPT authentication present, and no configured local MCP servers. User config `/home/serveradmin/.codex/config.toml` has trust for `/home/serveradmin` and no model override. This active thread's sandbox is `danger-full-access` with approval policy `never`; the CLI's default doctor probe reported `OnRequest` and restricted filesystem/network for its own invocation. These are distinct contexts.
- The bundled model catalog and [official OpenAI model page](https://developers.openai.com/api/docs/models/gpt-6-sol) identify `gpt-6-sol` and support `high` effort. A fresh CLI call explicitly requested `-m gpt-6-sol -c model_reasoning_effort="high"` without a model error; `codex doctor` with those overrides resolved provider `openai` and model `gpt-6-sol`. Effective wire model/effort for this parent session or the child was not exposed, so sol-6-high inheritance is **NOT_TESTED**, not asserted.
- No global `/home/serveradmin/.codex/AGENTS.md` or override, `/home/serveradmin/AGENTS.md` or override, or nested instructions were found. Root `AGENTS.md` is 4,606 bytes. `codex debug prompt-input` included it as the active project instruction, below the default 32 KiB discovery limit described in [official OpenAI AGENTS documentation](https://learn.chatgpt.com/docs/agent-configuration/agents-md). It points to the ledger, delivery state, PF-001 prompt, principal review and legacy exclusion. No alternate bridge or duplicated `CLAUDE.md` was needed.
- Two fresh `codex exec --ephemeral --skip-git-repo-check -s read-only` sessions received `AGENTS.md` in their prompts but could not perform shell file reads because sandbox initialization failed (`bwrap: loopback: Failed RTM_NEWADDR: Operation not permitted`). The first answered from injected instructions only; the second correctly declined to infer file contents. Fresh-session instruction injection is **VERIFIED**; live read-only file access is **FAILED** and needs host diagnosis before a fixed-ref reviewer can be claimed.
- One local child task read `docs/delivery/state.md:11` and `docs/architecture/decision-ledger.md:8,26`, returned PF-001 plus the locked legacy exclusion and principal-review rule, and made no edits or external calls. Child model/effort/tool inheritance was not observable. Its runtime reported `danger-full-access`, so prose-only read-only restrictions do not prove sandbox enforcement. No persistent custom role files were added to this neutral workspace; configure and test `orchestrator`, `implementation-worker`, `contract-scout`, `spec-reviewer` and `correctness-reviewer` under the verified rewrite repo using the installed host's documented mechanism.

## Capability evidence

The sanitized command/result record is [pf001-probes.md](evidence/pf001-probes.md); the table below is its readiness summary.

| Capability | State | Probe and limit |
|---|---|---|
| Git/worktrees | VERIFIED | Git `2.53.0`; scratch `git init`, empty commit, `worktree add/list/remove` succeeded. No user repo modified. |
| GitHub reads | VERIFIED | Connected app listed repositories and fetched candidate READMEs as `Optidigi-AK`. |
| GitHub local write path | NOT_TESTED | `gh 2.101.0` reports no logged-in host; target not approved. No branch, push or PR. |
| GitHub rules/review identity | NOT_TESTED | Connector login is `Optidigi-AK`; a separate native human reviewer, protection/ruleset enforcement and merge operator are not verified. The principal's chat verdict remains external and SHA-bound; the user is default merge operator. |
| Node/TypeScript | VERIFIED | Node `24.21.0` ran a disposable script; existing TypeScript `7.0.2` strictly compiled a disposable `.ts` file with `--noEmit`. |
| pnpm | VERIFIED | Corepack fetched pnpm `12.6.0`; disposable `pnpm run smoke` passed. No rewrite pin or lockfile exists. |
| Rust/Wasm | MISSING | `cargo`, `rustc`, `rustfmt`, `rustup`, clippy and `wasm32-unknown-unknown` unavailable on PATH; compile smoke cannot run. |
| Shopify developer MCP | VERIFIED | Disposable `@shopify/dev-mcp@1.15.4` over stdio listed actual `learn_shopify_api`, `search_docs_chunks`, `validate_graphql_codeblocks` tools; 2026-07 Cart Transform docs lookup succeeded. `query Input { cart { lines { id quantity } } }` validated; `definitelyInvalidPf001Field` was rejected as absent on `Cart`. This is schema evidence only. The package is transient; Codex has no persistent MCP server config. [Shopify AI Toolkit](https://shopify.dev/docs/apps/build/ai-toolkit). |
| Shopify authenticated app/store | MISSING | `shopify` CLI absent; no designated test app/store or read-only authenticated request. Development-store capability differences have not been measured. |
| Browser automation | VERIFIED | Existing Playwright opened a disposable static page, clicked a button, observed `clicked` in DOM and console, and captured [screenshot](evidence/pf001-browser.png). Chromium `153.0.8010.12`. Default launch failed on missing system `libatk-1.0.so.0`; retry passed with existing `/home/serveradmin/component-catalog/.pw-libs` in `LD_LIBRARY_PATH`. |
| PostgreSQL | MISSING | `psql` absent and no designated development connection; read-only server version/isolation check not performed. |
| R2 artwork storage | NOT_YET_REQUIRED | No named non-production bucket/prefix or read credentials; needed by M6. |
| Partner/App Pricing | NOT_YET_REQUIRED | No named test app/plan context or authenticated read; needed by G8. |
| CI/evidence scripts | NOT_CREATED | No approved rewrite workspace; no full-workspace scripts, workflow or CI runs to inspect. |
| Skills | NOT_TESTED | Five inspected upstream skills copied only to neutral `.agents/skills`. `codex debug prompt-input` listed four; `handoff` is user-invoked by upstream design and was not auto-listed. Rewrite-repo loading remains NOT_TESTED. |

## Skills and permissions

The five selected `mattpocock/skills` directories were inspected and copied, including referenced `tdd/tests.md`, `tdd/mocking.md`, `writing-for-agents/SKILL-MECHANICS.md`, role metadata and the diagnosing-bugs script. Exact upstream commit: `c55ee46073ed923f86ce59a5eb3b6d895095d1b7`; MIT license preserved at `.agents/skills/LICENSE.mattpocock`. Resolved local skill paths are `.agents/skills/{writing-for-agents,tdd,code-review,diagnosing-bugs,handoff}/SKILL.md`. [Official OpenAI skill discovery documentation](https://learn.chatgpt.com/docs/build-skills) describes this path. `handoff` contains upstream `disable-model-invocation: true`, so the model prompt omits it; its file was inspected, but an explicit invocation was not exercised. `writing-for-agents` informed the operational documents in this preflight. Their default interview, seam-confirmation, parallel-review, tracker or commit steps do not override PF-001 and the project operating model. `docs/agents/issue-tracker.md` already maps specs to the current prompt and future approved GitHub Issues. No other orchestration framework was installed.

Resources and permissions: no store, database, R2 bucket, billing plan or production resource was designated. No store mutation, deployment, paid event, repo protection change, privileged install, credential change, app scaffold or gate execution occurred. The temporary Node package and Corepack downloads did not change user/global configuration.

## Blockers and smallest remedies

| Blocker | Impact / next slice | Smallest remedy / owner |
|---|---|---|
| No verified user-approved rewrite repository and PR base | PF-001 publication; M0-001 cannot bind a base/head | User names the exact existing rewrite repository and confirms its default branch/purpose. If empty, maintainer creates its initial branch. Orchestrator then checks instructions/status before copying records. |
| Local `gh` has no login and GitHub review/protection state is unknown | Docs-only PR write path and merge controls | User/maintainer authenticates `gh` for only the approved repo, identifies native merge/review identity and provides readable ruleset/protection access or a manual status record. Orchestrator probes with the authorized docs branch/PR. |
| Sol-6-high effective runtime/child inheritance not observable; fresh CLI read-only file read failed | Trustworthy fixed-ref local review and delegated M0 work | Local host maintainer diagnoses Codex read-only sandbox and exposes/records effective model/effort and child permissions. Re-run a fresh read-only instruction/role probe in the approved repo. Do not substitute another model. |
| Rust/Cargo/Wasm target absent | M0 authorization/Function harness and G2/G3 | Local host maintainer installs a pinned Rust toolchain, rustfmt, clippy and wasm target without elevation or records approved installation path; orchestrator runs minimal compile. |
| Shopify CLI and designated app/store absent | M0 real-store G1/G4/G6/G7 and store-facing work | User designates non-production app/store and grants scoped local CLI access; orchestrator performs a read-only identity request and records plan/capability caveats. |
| Dedicated PostgreSQL 18 connection absent | DB-dependent M0 harness and later M3 | User designates isolated development DB and scoped credentials; local host supplies client or pg path; orchestrator performs read-only version/identity check. |
| R2 and Partner/App Pricing test contexts undesignated | M6 and G8 respectively | Resource owner provides named non-production resources and read access before those stages; no PF-001 mutation is needed. |

## Review and handoff

Changed from the supplied transfer snapshot: `docs/delivery/state.md`, `docs/delivery/tooling-register.md`; added this report, hash/probe evidence, browser screenshot and five project-scoped skills with license. Original architecture/decision records and root instructions were not edited. `MANIFEST.sha256` remains the original transfer manifest; current hashes are recorded separately. A fresh local reviewer found no instruction contradiction, apparent secret exposure or unauthorized app/gate work. It identified the missing local commit/patch and sparse retained probe evidence; these were addressed by the local handoff artifacts and [probe record](evidence/pf001-probes.md). It also observed that native read-only enforcement and model/effort are unverified. The reviewer itself had no native read-only sandbox guarantee. No fixed base/head diff review is possible until the rewrite repository is established.

Principal action: review this LOCAL_ONLY preflight and identify the approved rewrite destination. Once the access/toolchain blockers are resolved, authorize completion of PF-001 publication with exact repo/base/head/effective-merge-base refs. No M0-001 execution is authorized by this report.
