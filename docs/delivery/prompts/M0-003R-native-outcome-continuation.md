# M0-003R — resume the native lifecycle comparison

Principal-issued: 25 September 2026. **Version 1.1 — delegated execution package.** Orchestrator: sol-6-high using the verified explicit launch route. This replaces the unsent version 1.0 of this prompt. The native test matrix, protected resources, mutation ceiling and principal gate authority are unchanged; the update permits bounded local delegation and complete local execution. This is a continuation inside M0/G1, not a new preflight or architecture.

## Objective and supersession

Execute the previously unrun plain-control/expanded-candidate comparison and, only on success, its remaining native lifecycle checks. Preserve the old failure; determine the new workflow's actual outcome without requiring access to Shopify Admin's private request body.

**This prompt supersedes M0-003's unconditional native request-capture/abort prerequisite and any corresponding AGENTS or guard-use instruction.** Request capture is optional supplementary evidence when safely available. Its absence alone must not stop the bounded tests below. Exact line identity, quantity, native behavior and post-action evidence remain mandatory. No later result may be described as proof of an uncaptured original request or as establishing the old incident's root cause.

PR #5 is principal-approved at base/effective merge base `895aef65179267a9944c57c40478eeb704a8b65a`, head `afce3b668240d3a6b87741c173d1be0547ef4821`. The owner must separately authorize its normal merge and this staging envelope. The companion owner launch supplies both when sent by the owner. Verify exact refs/checks, preserve unrelated work, merge only when delegated, verify the actual remote result, and branch from the merged main. If already merged, verify the merged content and do not merge again. No force push, protection bypass, fabricated native approval or automatic next-PR merge.

Read AGENTS, ledger, operating model, this prompt, the accompanying review, M0-003's permission/resource table and C/R/B definitions, and its evidence index. Use the pinned diagnosing-bugs/TDD/code-review skills only where relevant; they do not require redoing discovery or reopening decisions. Update operational routing to M0-003R version 1.1 on the new branch. The execution section below replaces the old mandatory sequential-only/no-native-delegation instruction for this package. One serialized staging operator remains mandatory.

## Local execution and delegation — v1.1

Treat all of sections 1–4 as **one authorized work package**, not a sequence of principal approval requests. Decompose it into a short task/dependency list in the existing delivery state or PR description. Complete ordinary research, implementation, tests, integration, local-review corrections, evidence and cleanup before returning one outcome PR. A material invariant failure or missing permission still triggers the applicable stop rule; passing a local task does not grant new scope.

**Ownership:** ChatGPT in the Insignia Rewrite Project remains principal architect/reviewer, selects the work and adjudicates all gate evidence. The owner controls product decisions and resource/merge permissions. The local sol-6-high orchestrator owns execution tactics and integration within this package, not architectural acceptance or the next milestone. It cannot change a lock, weaken acceptance criteria, declare G1 passed, merge the next PR or begin G2–G8/M1. Local reviewers advise; every final PR still returns to the principal at exact refs.

**Use a small team where useful:** one orchestrator; one implementation writer by default, no more than two concurrent writers including the orchestrator if it is writing; up to two fresh read-only reviewers/scouts, preferably in review waves; delegation depth one. Workers use separate branches/worktrees and assigned, non-overlapping paths. The orchestrator integrates shared contracts, fixture definitions, manifests, CI, instructions and state. It alone operates the shared Shopify browser/app/store. No worker independently mutates staging or receives merge authority. Worktrees isolate edits, not credentials; do not claim security guarantees beyond observed enforcement.

Suitable internal tasks are (a) live-control projection/guard work and its tests, (b) offline receipt/inventory verification, and (c) final integration and independent Spec plus Standards/correctness reviews. A scout may check a specific documented behavior. Tasks start only when their actual inputs exist; offline code or review can run alongside the serialized staging operator without competing over its browser or receipts. Workers may inspect completed captures but must not rewrite them to satisfy assertions. Do not launch unrelated pricing, admin or billing workstreams just to use parallel capacity.

**Bounded runtime setup is permitted in this package.** Inspect the installed host's help/config schema. Use its supported per-launch or project-local settings to enable native delegation; verify the actual child's instructions, requested `gpt-6-sol`/`high` client settings and effective permissions. Start with one harmless spawn/join plus disk/skill read and appropriate boundary control; reuse unaffected PF-002 evidence. Native delegation must not expose mutation-capable connectors to a reviewer. Workers receive only task-required capabilities; shell isolation does not constrain separate MCP actions. If that native path is unavailable or cannot be safely bounded without host changes, record the limit and use the already verified separate restricted sessions. Do not turn optional delegation into another preflight blocker, relax isolation, change global trust/configuration or upgrade the host to force it to work.

**Context and skills:** all roles read the active prompt and relevant ledger/plan sections; worker briefs state the task, inputs, permitted paths/tools/resources, tests and stop conditions. Use the existing pinned `writing-for-agents`, `tdd`, `diagnosing-bugs`, `code-review` and `handoff` where applicable. `research`, `to-tickets` and `implement` may be inspected and added project-locally from `mattpocock/skills` at `c55ee46073ed923f86ce59a5eb3b6d895095d1b7` when they fill an actual need, with support files, license and provenance. Their decomposition/execution guidance is explicitly authorized only inside this package; it does not require a new scope interview or ticket for every subtask. Reuse the working Git/GitHub, build/test, Shopify documentation/schema, CLI and shared-browser routes. No blanket plugin/skill installation or credential expansion.

**Review and completion:** the orchestrator integrates and checks its work, then obtains fresh Spec and Standards/correctness reviews of the integrated fixed-ref diff. Use independent contexts, preserving each finding and its disposition. An implementer rereading its own work is not independent review. Run final checks after corrections; source-read-only review may execute tests in an explicitly isolated writable scratch environment where supported. Include the roles actually used and remaining limitations in the final packet. Keep setup/configuration changes minimal and in the same outcome PR, with operational pointers updated to this version. This is the pilot of broader local execution, not authority to create a separate workflow platform or auto-start later packages.

## Resource and budget boundaries

Use only existing app `insignia`, OAuth client `942e6668fd1177524c0fc48b104b0ac3`, org `212732011`, and `insignia-staging.myshopify.com` (shop `78935261342`). Reuse product `10294344482974`, Small variant `50529053343902` / inventory item `52644294131870`, Medium variant `50529054163102` / inventory item `52644294951070`, and Shop location `89465290910`. Full Shopify GIDs and existing mapping are in the preceding prompt/receipts.

**Order #1001 (`7184619962526`), fulfillment `6511606562974`, and their remaining commitments are read-only.** Latest recorded available/committed/on-hand: Small `8/2/10`; Medium `10/2/12`. Verify current values; they are observations, not reset targets. No stock seed, manual adjustment or rewriting prior receipts is allowed.

Owner-authorized scope: the existing fixture's temporary Online Store publication; existing app's dev preview and one owned allowlisted transform; dedicated synthetic carts; up to **three new successful Bogus orders and thirteen purchased units total** (C=4, R=6, conditional B=3); native fulfillment/refund/restock/cancellation of those NEW orders only; direct evidence; owned-resource cleanup. Carry any already-created orders forward against that budget; do not create replacements to conceal an ambiguous result. M0-003 reports none.

Keep current nine scopes; the app performs narrow reads and existing fixture/transform operations only. Use native Admin for order writes. Keep fixed catalog USD 20, customized USD 30, the Function/schema/operation unchanged, password protection enabled, and all real-payment/live-provider settings untouched. Confirm the currently active Bogus test-only route before checkout; if it cannot be established, request only that actual missing owner action. Historical gateway use is not current proof. No extra scopes, custom fulfillment API, internal API replay, app/store creation, distribution choice, released deploy, real payment, purchased label, actual shipment or unrelated mutation.

## 1. Minimal live readiness, not another tooling project

Verify the known authenticated browser, test shop/location, protected-order baseline, stock and test-payment route before relevant mutations. Reuse unchanged tools/tests. Existing inspection, native input/click and visual capture are sufficient for the following acceptance path. Do not repeat the failed ResourceTiming/body experiment or seek a new MCP/framework merely to clear the superseded condition.

The original outgoing request remains UNAVAILABLE unless a genuinely retained artifact already exists. Do not repeat open-ended recovery searches. Do not copy session cookies, patch fetch/XHR, install service-worker interception, expose debugging ports or export broad authenticated traffic. Normal read-only DOM inspection and cropped visual recording are permitted. Optional existing supported network observation must stay narrow, secret-free, unmodified and explicitly distinguished from an aborted or sent request.

**Done when:** known resources and protected baseline are verified, capture mode is recorded (normally `NATIVE_UI_PLUS_PUBLIC_API_RECEIPTS`), and the intended control experiment is ready. Private payload absence is not a blocker in this mode.

## 2. Pre-submit control: preserve meaning, calibrate to actual UI

The current guard is FORM_ONLY and always returns `submissionAuthorized:false`. Keep it separate from permission: owner authorization plus this runbook permits the action; an exit-zero guard does not. Its unconditional request-capture prose may be updated to this method. Do not just flip `submissionAuthorized` to true.

Use the actual new control order to observe native quantity semantics. Edit using supported visible controls/keyboard events, including blur/settling; page evaluation only reads. Set non-target quantities to zero where those controls apply. Do not assume that an unchecked row with a positive quantity is excluded. Do not assign DOM values through JavaScript or change framework state.

Immediately before the one-unit partial fulfillment, retain:

1. The exact intended order/fulfillment-order/line identity and one-unit quantity, plus complete direct API mapping of all relevant lines/groups and remaining quantities.
2. A fresh cropped screenshot or video and read-only DOM projection showing the full relevant native row set, effective quantities, actual applicable control properties, notifications-off state and aggregate native summary.
3. A checked unique mapping between native rows and actual order/FOL identities, using observed properties/markers/price and direct API context. Variant/SKU, row position, or invented IDs alone are insufficient. Parent/group presentation must not double-count a physical child.

All relevant rows must be accounted for. Intended target=1; every bystander=0; native aggregate=1; target identity and location must agree. If a UI summary is not an item quantity, establish and document its actual meaning on the control; do not compare unlike units. Missing identity or unresolved contradiction stops submission.

The synthetic guard currently assumes every row exposes checkbox and ARIA state. Do not manufacture these fields for real controls. A small projection/guard adaptation is allowed only from observed native semantics, with raw values/provenance retained and regression tests. Missing-but-not-applicable is different from unknown; when uncertain, fail the precheck. Verify mapping against the complete API row inventory, not just GID-shaped strings. Preserve rejection of the historical one-target/two-summary case. The first new partial-fulfillment check remains exactly one target unit.

With a coherent, freshly captured precondition and owner authorization, **submit once through native Admin even when request-body capture is unavailable**. Immediately read actual order, fulfillment-line IDs/quantities and inventory through the supported app API. Match the exact intended multiset and require all bystanders unchanged. For ambiguous timeout or missing result, read/reconcile first; never retry blindly.

If only browser automation cannot establish correct controls, one owner-assisted native interaction on the NEW synthetic order may be used in the same shared browser while the agent records evidence. Record actor and steps. This is human-operated native acceptance, not proof of automated capability, and is not a request for credentials or a new role setup.

**Done when:** live controls have been honestly mapped and tested, and the first native control result is either exact or a precisely captured contradiction. No internal request semantics are inferred.

## 3. Execute the same C/R/B comparison

### C: plain duplicate-variant control — 4 Small, USD 80 merchandise

Use inert differing public diagnostic properties for 3 target Small and 1 bystander Small, with neither recognized customization marker. Verify distinct order lines, no LineItemGroup/expansion, quantity 4 and USD 80 before test payment. Do not change variants or prices to separate them.

Perform exactly one target-unit native partial fulfillment under section 2. Direct receipts must show one intended line/unit only, with the bystander still unfulfilled. On success, use clearly reconciled native refund/restock/cancellation to undo this NEW control order's stock effect. Verify the resulting money and stock; cleanup is not evidence for customized refund pricing. Stop dependent work on any mismatch.

### R: original mixed candidate — 6 units, USD 170 merchandise

After C passes, create 3 marked Small at USD 30, 2 marked Medium at USD 30 and 1 plain Small at USD 20, using new run-specific markers with the unchanged Function. Capture actual cart/Function output and accepted order mappings. One native partial fulfillment must affect exactly one marked Small and no plain Small or Medium. Use the same supported quantity interaction and pre/post checks as C.

A coherent form followed by a wrong fulfillment is a reproduced merchant-workflow failure even without request capture. Stop dependent mutations, preserve it and escalate; do not claim which internal component caused it. Conversely, an exact result is evidence for this observed supported native procedure, not proof of the old incident's cause or universal correctness.

### Conditional remaining lifecycle — only after C and R exact partial results

Continue R with native remainder fulfillment, native calculated refund of one marked Small with restock, and remainder refund without restock. Use explicit line/quantity and refund-price checks for each action; the one-unit diagnostic helper is not an all-purpose authorizer for those later operations. Without discounts/tax/shipping on these fixtures, the one marked Small refund basis must be USD 30; verify actual context rather than type a convenient refund amount. Distinguish refund money, on-hand return and release of unfulfilled commitments.

The optional third order B is 3 marked Small, USD 90 merchandise, entirely unfulfilled before native cancellation/refund/restock. Budget remains three orders/thirteen purchased units. Check stock remains available; no reseed is allowed. Perform no mutation to #1001. Any mismatch stops its dependent steps; do not consume another order simply to fill a table.

**Done when:** the native comparison has an actual supported PASS/FAIL/INCONCLUSIVE result, plus honest outcomes for permitted dependent cases. A coherent-form failure is enough to return a reproducer; reverse-engineering the private API is not required. An unresolved automation mapping is not evidence that Shopify failed.

## 4. Preserve evidence, clean owned activation, return once

Before and after each native action, capture direct line-level and inventory projections with UTC times, exact observed IDs, requested vs actual quantities, actor/mode and capture limitations. Screenshots/videos must omit buyer details, credentials, cart/checkout secrets and unrelated Admin content. Avoid handwritten reconstructions of requests. Keep operator intent separate from actual observed data.

Keep historical `evidence/m0-002/` and `evidence/m0-003/` bytes/manifests unchanged. Put resumed-run observations in `evidence/m0-003r/`, including a compact matrix distinguishing `nativeWorkflowOutcome`, `requestCapture`, `historicalRootCause`, `publicAppQualification` and `gateStatus`. Preserve `historicalRootCause=UNDETERMINED` unless new evidence actually establishes it. Request capture may legitimately be `UNAVAILABLE_NOT_REQUIRED_FOR_THIS_TEST`.

Delete only this run's owned transform, stop/clean its preview, and unpublish/archive the fixture. Preserve the installation, nine scopes, password, unrelated versions/apps and #1001. No manual stock cleanup. List residual test orders, fulfillments, refunded/restocked quantities and committed stock truthfully. If a new native operation fails, preserve its state instead of compensating it into a passing record.

Keep existing tests and both historical manifests passing; add only the small live-projection and resumed-receipt tests required by this run. CI stays read-only/off-store and must include the new manifest/receipts when committed. Copy this prompt and attributed PR #5 verdict into delivery docs and record actual merge/current slice. Minimal updates to AGENTS, the operating model, agent-role/tooling pointers, selected skill files and project-local role configuration are allowed to make this package's execution policy effective. Keep their diff separately identifiable within this same outcome PR; do not rewrite the architecture plan/ledger or grant unnamed future work. A passing guard or manifest is not G1 progress without the native operation evidence.

Return one focused PR with refs, actual C/R/B outcomes, strongest supported conclusion and residue list. Overall G1 remains IN_PROGRESS pending principal adjudication and ordinary public-app/non-Plus qualification. G2–G8, M1 and architecture alternatives remain unauthorized. The historical incident and any new failure must remain visible.

## References and interpretation

Shopify's native partial-fulfillment documentation uses desired item quantities:
https://help.shopify.com/en/manual/fulfillment/fulfilling-orders/single-fulfillment

Supported network APIs exist in Playwright, but that does not mean this shared tab exposes them or that they are required here:
https://playwright.dev/docs/network
https://playwright.dev/docs/api/class-request

This is an explicit principal adjustment to experimental evidence collection, not a waiver of real-variant merchant semantics or an authorization to bypass platform/security controls.

Current primary references for host-specific role/skill setup (check the installed version, not examples from a different release):
https://developers.openai.com/codex/subagents
https://developers.openai.com/codex/skills
https://github.com/mattpocock/skills/tree/c55ee46073ed923f86ce59a5eb3b6d895095d1b7
