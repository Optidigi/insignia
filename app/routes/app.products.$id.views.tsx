/**
 * Product Config Views Layout
 * 
 * Renders nested views routes (list, detail).
 */

import { Outlet } from "react-router";

export default function ProductViewsLayout() {
  return <Outlet />;
}
